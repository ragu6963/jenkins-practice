
pipeline {
    agent any
    stages { 
        stage('Use Injected Variables') {
            steps {
                script {
                    // 주입된 환경 변수 사용
                    echo "MultiBranch 5"
                }
            }
        }
    }
}
